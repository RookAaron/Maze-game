The maze will be generated using the recursive backtracking algorithm

The objective is to reach the far left wall then the far right wall as many times as possible.
The game ends once the user has been caught by a bot

In C++ using visual studios
